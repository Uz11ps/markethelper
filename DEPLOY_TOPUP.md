# Инструкция по применению функционала пополнения баланса

## Что было добавлено

1. **Кнопка "Пополнить" в боте** - добавлена в главное меню
2. **Обработчик пополнения** - пользователь может выбрать сумму пополнения
3. **API endpoint** - `/api/tokens/purchase` для создания заявки на пополнение
4. **Админ-панель** - страница `/topup.html` для управления заявками на пополнение

## Команды для применения на сервере

### 1. Подключитесь к серверу
```bash
ssh root@iawuuw.com
```

### 2. Перейдите в директорию проекта
```bash
cd /root/markethelper
```

### 3. Остановите контейнеры
```bash
docker-compose -f docker-compose.prod.yml down
```

### 4. Получите последние изменения из репозитория
```bash
git pull origin main
# или
git pull origin master
```

### 5. Пересоберите и запустите контейнеры
```bash
docker-compose -f docker-compose.prod.yml up -d --build
```

### 6. Проверьте логи бота (чтобы убедиться, что обработчик зарегистрирован)
```bash
docker logs markethelper-bot-prod --tail 50
```

### 7. Проверьте логи backend (чтобы убедиться, что API работает)
```bash
docker logs markethelper-backend-prod --tail 50
```

## Проверка работы

### В боте:
1. Откройте бота в Telegram
2. Нажмите кнопку "💰 Пополнить"
3. Выберите сумму пополнения
4. Должна создаться заявка

### В админ-панели:
1. Откройте `https://iawuuw.com/topup.html`
2. Войдите в админ-панель
3. Должны отображаться все заявки на пополнение
4. Можно одобрить или отклонить заявку

## Структура изменений

### Backend:
- `backend/api/tokens.py` - добавлен endpoint `/api/tokens/purchase`
- `backend/api/admin_tokens.py` - уже существовал, используется для управления заявками

### Bot:
- `bot/keyboards/main_menu.py` - добавлена кнопка "💰 Пополнить"
- `bot/handlers/topup.py` - новый обработчик пополнения
- `bot/services/api_client.py` - добавлен метод `create_token_purchase_request`
- `bot/app.py` - зарегистрирован роутер `topup`

### Frontend:
- `frontend/topup.html` - новая страница для управления заявками
- `frontend/topup.js` - логика работы со страницей
- `frontend/style.css` - добавлены стили для статусов
- Обновлена навигация во всех страницах админки

## Возможные проблемы

### Если бот не отвечает на кнопку "Пополнить":
1. Проверьте логи бота: `docker logs markethelper-bot-prod --tail 100`
2. Убедитесь, что роутер `topup` зарегистрирован в `bot/app.py`
3. Перезапустите бота: `docker restart markethelper-bot-prod`

### Если API не работает:
1. Проверьте логи backend: `docker logs markethelper-backend-prod --tail 100`
2. Убедитесь, что роутер `tokens` подключен в `backend/app.py`
3. Перезапустите backend: `docker restart markethelper-backend-prod`

### Если страница админки не открывается:
1. Проверьте, что файл `frontend/topup.html` скопирован на сервер
2. Проверьте права доступа: `chmod 644 frontend/topup.html`
3. Перезапустите frontend контейнер: `docker restart markethelper-frontend-prod`

## Дополнительные настройки

### Изменение вариантов пополнения

Чтобы изменить суммы пополнения, отредактируйте файл `bot/handlers/topup.py`:

```python
TOPUP_OPTIONS = [
    {"tokens": 100, "price": 100, "label": "100 токенов - 100₽"},
    {"tokens": 250, "price": 225, "label": "250 токенов - 225₽ (скидка 10%)"},
    # Добавьте свои варианты
]
```

После изменения пересоберите и перезапустите контейнер бота.

## Готово!

После выполнения всех команд функционал пополнения баланса будет доступен в боте и админ-панели.

