# Решение проблемы с кнопкой "Пополнить"

## Проблема
Кнопка "💰 Пополнить" не появляется в боте даже после пересборки контейнеров.

## Причина
Telegram кэширует клавиатуру у пользователя. Чтобы обновить её, нужно отправить новую клавиатуру через API бота.

## Решение

### Вариант 1: Написать команду в боте (САМЫЙ ПРОСТОЙ)

После пересборки контейнеров, **в боте напишите одну из команд:**

1. `/update_keyboard` - специальная команда для обновления клавиатуры
2. `/menu` - вернет в главное меню с новой клавиатурой  
3. `/start` - перезапустит бота и покажет новую клавиатуру

**После этого кнопка "💰 Пополнить" появится!**

### Вариант 2: Проверка на сервере

```bash
# 1. Проверьте что код обновился
docker exec markethelper-bot-prod cat /app/bot/keyboards/main_menu.py | grep "Пополнить"

# Должно показать:
# [KeyboardButton(text="💰 Пополнить")],

# 2. Проверьте что обработчик зарегистрирован
docker exec markethelper-bot-prod grep -r "keyboard_update" /app/bot/app.py

# Должно показать:
# from bot.handlers import keyboard_update
# dp.include_router(keyboard_update.router)

# 3. Проверьте логи бота
docker logs markethelper-bot-prod --tail 50

# 4. Перезапустите бота если нужно
docker restart markethelper-bot-prod
```

### Вариант 3: Принудительное обновление через API (для админа)

Если у вас есть доступ к API бота, можно отправить сообщение с новой клавиатурой через API.

## Важно!

**Клавиатура НЕ обновится автоматически!** Пользователю нужно:
- Написать `/start` или `/menu` или `/update_keyboard` в боте
- Или нажать кнопку "👤Профиль" - там тоже обновится клавиатура

## Проверка что все работает

1. Напишите `/start` в боте
2. Должна появиться кнопка "💰 Пополнить" в главной клавиатуре
3. Нажмите на неё - должно открыться меню пополнения

## Если все еще не работает

1. Убедитесь что контейнеры пересобраны: `docker-compose -f docker-compose.prod.yml up -d --build`
2. Проверьте логи: `docker logs markethelper-bot-prod --tail 100`
3. Перезапустите бота: `docker restart markethelper-bot-prod`
4. Напишите `/start` в боте заново

