# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Кнопка не появляется

## Проблема
Кнопка "💰 Пополнить" не появляется даже после всех обновлений.

## ВАЖНО: Проверьте что вы написали команду в боте!

Telegram **НЕ обновляет клавиатуру автоматически**. Вы ДОЛЖНЫ написать команду в боте!

### Шаги для решения:

1. **На сервере** (уже сделано):
```bash
git pull origin main
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build
```

2. **В Telegram-боте** (ОБЯЗАТЕЛЬНО!):
   - Откройте бота MarketHelper
   - Напишите `/start` 
   - **Проверьте кнопки** - должна быть "💰 Пополнить"
   - Если не появилась - напишите `/update_keyboard`
   - Если все еще не появилась - напишите `/menu`

3. **Если кнопка все еще не появилась:**
   - Полностью закройте Telegram (не просто сверните)
   - Откройте Telegram заново
   - Откройте бота
   - Напишите `/start`

## Диагностика на сервере:

```bash
# Проверьте что код правильный
docker exec markethelper-bot-prod cat /app/bot/keyboards/main_menu.py | grep "Пополнить"

# Должно показать:
# [KeyboardButton(text="💰 Пополнить")],

# Проверьте логи когда пользователь пишет /start
docker logs markethelper-bot-prod -f

# В другом терминале напишите /start в боте
# В логах должны быть сообщения о обработке команды
```

## Альтернативное решение:

Если кнопка все еще не появляется, можно добавить её в inline-клавиатуру профиля. Но сначала убедитесь что вы:
1. Написали `/start` в боте
2. Полностью перезапустили Telegram
3. Проверили что код правильный в контейнере

