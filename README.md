# MarketHelper Bot

Telegram-бот для управления маркетплейсами с функциями генерации контента, управления доступами и админ-панелью.

## Возможности

- 🤖 **Генератор контента** - ИИ-ассистент на основе GPT с векторной базой знаний
- 🎨 **Генерация инфографики** - создание изображений через FAL AI (nano-banana)
- 📁 **Управление доступами** - система выдачи cookie-файлов для групп пользователей
- 💰 **Монетизация** - система токенов, бонусов и реферальная программа
- 👥 **Админ-панель** - веб-интерфейс для управления ботом
- 📊 **Подписки** - управление тарифами и заявками на доступ

## Структура проекта

```
markethelper/
├── backend/          # FastAPI backend
│   ├── api/         # API endpoints
│   ├── models/      # Database models
│   ├── services/    # Business logic
│   └── schemas/     # Pydantic schemas
├── bot/             # Telegram bot
│   ├── handlers/    # Message handlers
│   ├── keyboards/   # Bot keyboards
│   └── services/    # Bot services
├── frontend/        # Admin panel (HTML/JS)
└── chroma_db/       # Vector database storage
```

## Требования

- Python 3.12+
- Docker и Docker Compose (для Docker-запуска)
- Telegram Bot Token
- OpenAI API Key (для GPT функций)
- FAL API Key (для генерации изображений)

## Развертывание на сервере

Подробная инструкция по развертыванию на продакшн сервере находится в файле [DEPLOY.md](DEPLOY.md) и [QUICK_START.md](QUICK_START.md).

**Быстрый старт:**
1. Подключитесь к серверу: `ssh root@80.76.43.152`
2. Выполните настройку: `./setup-server.sh`
3. Загрузите проект в `/opt/markethelper`
4. Настройте `.env` файл
5. Запустите: `./deploy.sh`

Админ-панель будет доступна по адресу: **https://374504.vm.spacecore.network**

## Быстрый старт

### Локальный запуск

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd markethelper
   ```

2. **Создайте файл `.env`** в корне проекта:
   ```env
   # Telegram Bot
   BOT_TOKEN=your_bot_token_here
   
   # Backend
   BACKEND_URL=http://localhost:8000
   
   # OpenAI
   OPENAI_API_KEY=your_openai_key_here
   
   # FAL AI
   FAL_API_KEY=your_fal_key_here
   FAL_API_BASE_URL=https://fal.run
   
   # Admin
   ADMIN_TG_ID=your_telegram_id
   JWT_SECRET_KEY=your_secret_key_here
   ```

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите Backend**
   ```bash
   python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
   ```

5. **Запустите Bot** (в отдельном терминале)
   ```bash
   python -m bot.app
   ```

6. **Откройте админ-панель**
   - Frontend: откройте `frontend/index.html` в браузере
   - Backend API docs: http://localhost:8000/docs

### Docker запуск

1. **Создайте файл `.env`** (см. выше)

2. **Запустите все сервисы**
   ```bash
   docker-compose up -d
   ```

3. **Проверьте статус**
   ```bash
   docker-compose ps
   ```

4. **Просмотр логов**
   ```bash
   # Все сервисы
   docker-compose logs -f
   
   # Только backend
   docker-compose logs -f backend
   
   # Только bot
   docker-compose logs -f bot
   ```

5. **Остановка**
   ```bash
   docker-compose down
   ```

## Переменные окружения

| Переменная | Описание | Обязательно |
|-----------|----------|-------------|
| `BOT_TOKEN` | Telegram Bot Token | Да |
| `BACKEND_URL` | URL backend API | Да |
| `OPENAI_API_KEY` | OpenAI API ключ | Да |
| `FAL_API_KEY` | FAL AI API ключ | Да |
| `ADMIN_TG_ID` | Telegram ID администратора | Нет |
| `JWT_SECRET_KEY` | Секретный ключ для JWT | Да |
| `DATABASE_URL` | URL базы данных (SQLite по умолчанию) | Нет |

## API Endpoints

### Backend API (http://localhost:8000/api)

- `/api/docs` - Swagger документация
- `/api/profile/{tg_id}` - Профиль пользователя
- `/api/admin/` - Админ endpoints
- `/api/ai/query` - Запрос к ИИ
- `/api/files/` - Управление файлами
- `/api/tokens/` - Управление токенами

### Bot API (http://localhost:8001)

- `/notify` - Уведомления пользователям
- `/broadcast` - Рассылка сообщений

## Разработка

### Структура базы данных

Проект использует Tortoise ORM с SQLite. Модели находятся в `backend/models/`.

### Добавление новых handlers

1. Создайте файл в `bot/handlers/`
2. Зарегистрируйте router в `bot/app.py`

### Добавление API endpoints

1. Создайте router в `backend/api/`
2. Подключите в `backend/app.py`

## Troubleshooting

### Проблемы с зависимостями

Если возникают конфликты версий:
```bash
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### Проблемы с Docker

Очистите кэш и пересоберите:
```bash
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

### Бот не получает обновления

Убедитесь, что запущен только один экземпляр бота. Проверьте процессы:
```bash
# Windows
tasklist | findstr python

# Linux/Mac
ps aux | grep python
```

## Лицензия

Proprietary - все права защищены

## Контакты

Для вопросов и поддержки обращайтесь к разработчикам проекта.

