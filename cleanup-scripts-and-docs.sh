#!/bin/bash
# Скрипт для очистки всех .sh и .md файлов на сервере

echo "🧹 Очистка всех .sh и .md файлов..."

# Переход в директорию проекта
cd /opt/markethelper || exit 1

# Поиск и удаление всех .sh файлов (кроме самого этого скрипта)
echo "📝 Удаление .sh файлов..."
find . -name "*.sh" -type f ! -name "cleanup-scripts-and-docs.sh" -exec rm -v {} \;

# Поиск и удаление всех .md файлов
echo "📄 Удаление .md файлов..."
find . -name "*.md" -type f -exec rm -v {} \;

echo ""
echo "✅ Очистка завершена!"
echo ""
echo "📋 Оставшиеся .sh файлы:"
find . -name "*.sh" -type f 2>/dev/null | head -10

echo ""
echo "📋 Оставшиеся .md файлы:"
find . -name "*.md" -type f 2>/dev/null | head -10

