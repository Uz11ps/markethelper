# Тестирование клавиатуры

## Проверка на сервере

Выполните эти команды для диагностики:

```bash
cd /opt/markethelper

# 1. Проверьте что код обновился
git pull origin main

# 2. Пересоберите
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build

# 3. Проверьте код в контейнере
docker exec markethelper-bot-prod cat /app/bot/keyboards/main_menu.py

# 4. Проверьте что обработчик topup зарегистрирован
docker exec markethelper-bot-prod grep "topup" /app/bot/app.py

# 5. Проверьте логи бота на ошибки
docker logs markethelper-bot-prod --tail 100 | grep -i error

# 6. Проверьте что бот отвечает на команды
docker logs markethelper-bot-prod --tail 50 | grep -i "start\|menu\|update"
```

## В боте (ОБЯЗАТЕЛЬНО!)

После пересборки контейнеров:

1. **Напишите `/start`** в боте
2. **Проверьте кнопки** - должна быть кнопка "💰 Пополнить"
3. Если не появилась - **напишите `/update_keyboard`**
4. Если все еще не появилась - **напишите `/menu`**

## Если кнопка все еще не появляется:

Возможные причины:
1. Telegram кэширует клавиатуру очень агрессивно
2. Нужно полностью закрыть и открыть бота заново
3. Может быть проблема с версией aiogram

Попробуйте:
- Полностью закрыть Telegram и открыть заново
- Написать `/start` несколько раз
- Проверить версию Telegram (обновить если старая)

